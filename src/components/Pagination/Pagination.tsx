import React from 'react';

class Pagination extends React.Component {
  constructor(props: object) {
    super(props);
  }
  render() {
    return <div>{/* <Pagination   defaultCurrent={1} total={50} /> */}</div>;
  }
}

export default Pagination;
